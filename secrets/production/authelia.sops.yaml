# Authelia Secrets — SOPS Encrypted
# ===================================
# Encrypt with:  sops --encrypt --in-place secrets/production/authelia.sops.yaml
# Decrypt with:  sops --decrypt secrets/production/authelia.sops.yaml

# -- JWT Secret for password reset tokens (min 64 chars)
# jwt_secret: ""

# -- Session encryption/authentication secret (min 64 chars)
# session_secret: ""

# -- Storage backend encryption key (min 32 chars, hex-encoded 32 bytes)
# storage_encryption_key: ""

# -- SMTP password (if notifier is configured for SMTP)
# smtp_password: ""

# Metadata (not encrypted)
purpose: "Authelia runtime secrets"