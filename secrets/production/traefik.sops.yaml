# Traefik Secrets — SOPS Encrypted
# =================================
# Encrypt with: sops --encrypt --in-place secrets/production/traefik.sops.yaml
# Decrypt with: sops --decrypt secrets/production/traefik.sops.yaml

# ACME email (not a secret, just referenced here for centralised config)
# acme_email: "admin@circuithq.internal"

# Basic auth users for Traefik dashboard (if IP whitelist is insufficient)
# credentials are of the form: user:hashed-password
# Generate with: htpasswd -nb user password | sed 's/\$/\$\$/g'
# dashboard_users: ""