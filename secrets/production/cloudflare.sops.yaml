# Cloudflare Tunnel & API Secrets — SOPS Encrypted
# ===================================================
# Encrypt with: sops --encrypt --in-place secrets/production/cloudflare.sops.yaml
# Decrypt with: sops --decrypt secrets/production/cloudflare.sops.yaml
# View:         sops secrets/production/cloudflare.sops.yaml

# cloudflare_tunnel_token: <encrypted>
# This is the token from Zero Trust Dashboard → Networks → Tunnels → circuithq → Token
# Format: "eyJhIjoi..." (base64-encoded JWT)
# tunnel_token: ""

# cloudflare_api_token: <encrypted>
# Scoped API token for DNS record management (Zone:DNS:Edit)
# Create in: Cloudflare Dashboard → My Profile → API Tokens
# api_token: ""

# email_address: "admin@circuithq.internal"
# Note: not encrypted, just metadata