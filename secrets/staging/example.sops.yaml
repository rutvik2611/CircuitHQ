# Staging Example Secrets — SOPS Encrypted
# ==========================================
# Template for staging environment secrets.
# Encrypt with: sops --encrypt --in-place secrets/staging/example.sops.yaml
# Decrypt with: sops --decrypt secrets/staging/example.sops.yaml
#
# Copy this file for each staging service:
#   cp secrets/staging/example.sops.yaml secrets/staging/<service>.sops.yaml

# Example placeholder — replace with actual fields for each service
# api_key: ""
# database_password: ""
# jwt_secret: ""

# Metadata (not encrypted)
environment: "staging"
purpose: "Example template — copy and rename for actual services"